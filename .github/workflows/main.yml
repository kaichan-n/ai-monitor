name: AI Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリを取得
        uses: actions/checkout@v4

      - name: Pythonをセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: AI監視エージェントを実行
        run: python main.py
